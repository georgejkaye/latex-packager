name: Update submodules
description: Make sure the submodules mirror what's in the prime versions
runs:
  using: "composite"
  steps:
    - name: Update submodules
      run: |
        git submodule git reset --hard origin/main
        git submodule foreach git pull origin main